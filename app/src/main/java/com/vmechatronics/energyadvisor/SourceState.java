package com.vmechatronics.energyadvisor;

/*
import android.util.Log;

import com.android.volley.Request;
import com.android.volley.Response;
import com.android.volley.toolbox.StringRequest;

import java.util.HashMap;
import java.util.Map;

public class SourceState extends StringRequest{
    public static final String REGISTER_REQUEST_URL = "https://vmechatronics.com/app/src_state.php";
    private static final String TAG = "Source State";
    private Map<String,String> params;
    public SourceState(String state, Response.Listener<String> listener)
    {
        super(Request.Method.POST, REGISTER_REQUEST_URL, listener, null);
        params = new HashMap<>();
        params.put("state", state);
        Log.w(TAG, "State is  " + state);
    }

    @Override
    public Map<String, String> getParams() {
        return params;
    }
}*/
