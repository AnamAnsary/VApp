package com.vmechatronics.energyadvisor;

/**
 * Created by root on 10/3/17.
 */

/*
import android.util.Log;

import com.android.volley.Request;
import com.android.volley.Response;
import com.android.volley.toolbox.StringRequest;

import java.util.HashMap;
import java.util.Map;

public class GetFilename extends StringRequest {
    public static final String REGISTER_REQUEST_URL = "https://www.vmechatronics.com/app/get_Filename.php";
    private static final String TAG = "GetFilename";
    private Map<String,String> params;
    public GetFilename(String email,String qid, Response.Listener<String>listener)
    {
        super(Request.Method.POST, REGISTER_REQUEST_URL, listener, null);
        Log.w(TAG, "GetFilename: " +email +qid);
        params = new HashMap<>();
        params.put("email", email);
        params.put("qid",qid);
        //params.put("filename", filename);
    }
    @Override
    public Map<String,String> getParams() {
        return params;
    }
}
*/
