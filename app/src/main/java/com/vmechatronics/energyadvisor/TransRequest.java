package com.vmechatronics.energyadvisor;

/*
import android.util.Log;

import com.android.volley.Response;
import com.android.volley.toolbox.StringRequest;

import java.util.HashMap;
import java.util.Map;

public class TransRequest extends StringRequest{
    public static final String REGISTER_REQUEST_URL = "https://vmechatronics.com/app/get_trans.php";
    private static final String TAG = "TransRequest";
    private Map<String,String> params;
    public TransRequest(String email, Response.Listener<String> listener)
    {
        super(Method.POST, REGISTER_REQUEST_URL, listener, null);
        params = new HashMap<>();

        params.put("email", email);
        Log.w(TAG, "Trans Request  " + email);
    }

    @Override
    public Map<String, String> getParams() {
        return params;
    }
}
*/
