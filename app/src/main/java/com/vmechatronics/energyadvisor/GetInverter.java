package com.vmechatronics.energyadvisor;

/*
import android.util.Log;

import com.android.volley.Response;
import com.android.volley.toolbox.StringRequest;

import java.util.HashMap;
import java.util.Map;

public class GetInverter extends StringRequest {
    public static final String REGISTER_REQUEST_URL = "https://www.vmechatronics.com/app/readdbase.php";
    private static final String TAG = "TransRequest";
    private Map<String,String> params;
    public GetInverter(String solType, Response.Listener<String> listener)
    {
        super(Method.POST, REGISTER_REQUEST_URL, listener, null);
        params = new HashMap<>();

        params.put("solType", solType);
        Log.w(TAG, "GetInverter  " + solType);
    }

    @Override
    public Map<String, String> getParams() {
        return params;
    }
}
*/
